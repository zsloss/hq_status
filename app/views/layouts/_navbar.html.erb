<nav class="navbar navbar-default">
	<div class="container-fluid">
	<div class="navbar-header">
			<%= link_to "Documentation Task Manager", root_url, class: "navbar-brand" %>
			</div>
<div id="navbar" class="navbar-collapse collapse">
			
			<ul class="nav navbar-nav navbar-right">
				<% if logged_in? %>
				<li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">Welcome <strong><%= current_user.name %> </strong><span class="caret"></span></a>
				<ul class="dropdown-menu" role="menu">
				<% if logged_in? %>
				<li><%= link_to "Task List", current_user %></li>
					<li><%= link_to "Edit User Details", edit_user_path(current_user) %></li>
					<% if current_user.admin %>
					<li class="divider"></li>
						<li><%= link_to "Tasks Admin", tasks_path %></li>
						<li><%= link_to "Users Admin", users_path %></li>
					<% end %>
				<% end %>
			</ul>
				</li>
				<li><%= button_to "Log Out", logout_path, method: "delete", class: "btn btn-default navbar-btn" %></li>
				<% else %>
					<li><%= button_to "Sign Up", new_user_path, method: "get", class: "btn btn-default navbar-btn" %></li>
					<li><%= button_to "Log In", login_path, method: "get", class: "btn btn-default navbar-btn" %></li>
				<% end %>
			</ul>
			</div>
</div>
</nav>